export enum mainTabValues {
  games = 'games',
  reviews = 'reviews',
  consumers = 'consumers',
  carts = 'carts',
  cartItems = 'cartItems',
  librarys = 'libraries',
  gemeGenres = 'gameGenres',
}
